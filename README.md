# travel-blog

private template for vitepress repo

1. Create a new repo using this template
2. run the app locally using `./scripts/localhost.sh start` and then go to `Setup` on top navbar.
3. follow to instructions to get started with the new repo.
